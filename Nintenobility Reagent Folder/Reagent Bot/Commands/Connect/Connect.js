
// const DClient = require("../../Clients/DClient")

// async function Connect(Platform, userToAdd){
//     let hasProfile = await Profile.findOne({discordid: userToAdd.id})

//     if(hasProfile){
//         DClient.channels.cache.get(Platform.message.channelId).send(`You already have an account connected under the Twitch username: ${profile.}`)
//     }
// }

// module.exports = Connect
